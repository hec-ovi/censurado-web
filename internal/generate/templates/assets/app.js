// Censurado client bootstrap. Placeholder; the real Tier-B refiner arrives in a
// later workflow. It will read the manifest (inline #cnz-manifest on landings or
// the standalone /manifest/<scope>/index.json), fetch month shards, and filter
// the [data-articles] list in place using the data-author/section/topics/month
// hooks on each [data-article-item]. Stable URL: /assets/app.js.
(function () {
  "use strict";
})();
